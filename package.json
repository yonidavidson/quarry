{
  "name": "quarry",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@genex-ai/cli-demo": "^0.93.0",
    "@types/three": "^0.185.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "@genex-ai/embed-sdk": "^0.11.0",
    "@genex-ai/multiplayer": "^0.13.0",
    "@pixiv/three-vrm": "^3.5.5",
    "three": "^0.185.1"
  },
  "description": "A 3D hunt-or-be-hunted browser game — three.js on Genex.",
  "genex": {
    "matchmaking": {
      "preset": "open",
      "config": {
        "maxPlayers": 2,
        "minPlayers": 2,
        "fill": 1
      }
    }
  }
}
