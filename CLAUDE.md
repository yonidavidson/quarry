# QUARRY

**All agent instructions live in [`AGENTS.md`](./AGENTS.md).**  
Read that file at session start — bus protocol, how to run/playtest, code map, pipelines, ship loop.

This file exists so Claude Code auto-loads the project guide; keep `AGENTS.md` as the source of truth for every harness (OpenCode, Claude, Cursor, etc.).
